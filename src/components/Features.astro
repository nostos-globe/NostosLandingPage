
---
const howItWorks = [
  {
    title: "Get Started & Connect",
    description: "Create your account and join a global community of travelers. Follow others, like albums, and share your own adventures.",
    icon: "/icons/user-connect.png"
  },
  {
    title: "Capture, Organize & Share",
    description: "Upload photos, build stunning albums, and relive your journeys. Organize your memories with tags and locations, and easily share or collaborate on albums with friends. Add your personal touch with captions, maps, and stories to bring every trip to life.",
    icon: "/icons/photo-share.png"
  },
  {
    title: "Visualize & Track Your World",
    description: "See your travels on a 3D interactive globe, explore shared journeys, and track how much of the world you've discovered.",
    icon: "/icons/global-track.png"
  }
];
---

<section id="features" class="features section relative overflow-hidden bg-[#1849FF]">
  <div class="absolute inset-0 bg-gradient-to-br from-blue-600 via-blue-500 to-blue-400 backdrop-blur-3xl"></div>
  <div class="absolute inset-0 bg-white/5 backdrop-blur-2xl"></div>
  <div class="container relative z-10">
    <div class="text-center mb-16 pt-24">
      <h2 class="text-3xl md:text-4xl font-bold text-white font-outfit features-title" data-aos="fade-up">How It Works</h2>
    </div>
    <div class="grid md:grid-cols-12 gap-8 pb-32">
      {howItWorks.map((item, index) => (
        <div 
          class={`feature-card p-10 backdrop-blur-xl rounded-3xl transition-transform transform duration-300 hover:scale-105 border border-white/20 shadow-lg 
            ${index === 0 ? 'md:col-span-4 h-[300px] mt-15 bg-white/10' : 
              index === 1 ? 'md:col-span-4 h-[420px] bg-white/10' : 
              'md:col-span-4 h-[300px] mt-15 bg-white/10'}`}
          data-aos="fade-up"
          data-aos-delay={`${index * 100}`}
        >
          <div class="inline-flex items-center justify-center w-15 h-15 rounded-full bg-white/10 text-white text-xl font-semibold mb-6 border border-white/30 shadow-sm">
            <img src={item.icon} alt={item.title} class="w-6 h-6"/>
          </div>
          <h3 class="text-xl font-semibold text-white mb-3 font-outfit tracking-wide feature-title-section">{item.title}</h3>
          <p class="text-white/80 font-outfit text-base leading-relaxed feature-text">{item.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
